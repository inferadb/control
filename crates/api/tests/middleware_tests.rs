//! Middleware integration tests (removed).
//!
//! The old session + organization + vault middleware has been replaced by JWT middleware.
//! Integration tests for JWT middleware are in the auth flow test suites.
