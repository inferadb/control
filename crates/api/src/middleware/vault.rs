//! Vault middleware (removed).
//!
//! Vault access checks are now performed inline in handlers via Ledger SDK calls.
//! The old middleware that extracted `VaultContext` from session state has been removed.
