//! Permission middleware (removed).
//!
//! Organization permission checks are now performed inline in handlers
//! via Ledger SDK calls. The old middleware that resolved team-based
//! permissions from the storage backend has been removed.
