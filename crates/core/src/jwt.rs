//! JWT signing utilities.
//!
//! Vault token signing is now handled by Ledger's token service.
//! User session JWT validation is handled by the API middleware via
//! Ledger SDK's `validate_token()`.
